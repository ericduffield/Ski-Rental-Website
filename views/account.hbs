{{#if message}}
<h1>{{message}}</h1>
{{/if}}


<div class="accountEdits">
    <h1>Welcome to your Account</h1>
</div>

<div>
    <div class="accountInfo">
        
    </div>
</div>

<div class="logoutAccount">
    <form action="/logout" method="post">
        <button class="btn btn-primary buttonRent" name="foo" value="upvote">Logout</button>
    </form>
</div>
<script>
    const model = require("../models/skiEquipmentModelMysql")
    // get the current cookie session.id
    const sessionId = document.cookie.split("=")[1];
    // get the current user's id
    const userId = sessionId.split("%")[0];

    let user = await model.getRentalFromUserId(userId);
    console.log(user);
    
</script>